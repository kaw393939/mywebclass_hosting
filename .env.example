# Cloudflare API Tokens for DNS Challenge
# Get these from: https://dash.cloudflare.com/profile/api-tokens
CF_DNS_API_TOKEN=your_cloudflare_dns_api_token_here
CF_ZONE_API_TOKEN=your_cloudflare_zone_api_token_here

# Traefik Dashboard Credentials
# Generate with: echo $(htpasswd -nb admin your_password) | sed -e s/\\$/\\$\\$/g
# Or use: docker run --rm httpd:2.4-alpine htpasswd -nbB admin "your_password" | sed -e s/\\$/\\$\\$/g
TRAEFIK_DASHBOARD_CREDENTIALS=admin:$$apr1$$hash$$here
